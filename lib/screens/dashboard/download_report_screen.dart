import 'package:flutter/material.dart';

class DownloadReportScreen extends StatelessWidget {
  const DownloadReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Download My Report')),
      body: const Center(child: Text('Download My Report Screen')),
    );
  }
}
